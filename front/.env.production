# Committed on purpose: no secrets, just the public production origin.
# Used automatically by `vite build` (mode defaults to "production").
VITE_API_BASE_URL=https://youssifallam.com
VITE_CONTACT_ENDPOINT=https://youssifallam.com/api/portfolio/contact/
