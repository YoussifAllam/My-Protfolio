venv/
Venv/
__pycache__/
__pycache__
/__pycache__
*.pyc


staticfiles
# User-uploaded/generated content (project covers, gallery images, the site
# logo) — runtime data, not source. Three synthetic test images had slipped
# in here before this rule existed; removed in the same commit that adds it.
media
desktop.ini
celerybeat-schedule
.mypy_cache
celery_db
data
db_volume
debug.log
volumes
code
venv
backend_venv


# LOGS/*
!LOGS/.gitkeep
django.log
debug.log
cacheops.log

build
dist
/.spec

# Migrations are COMMITTED on purpose. They are part of the schema's history: without them a
# second clone cannot reproduce the database, and two developers generate conflicting ones.

.env.local
.env.prod
.env.test
.env

myenv